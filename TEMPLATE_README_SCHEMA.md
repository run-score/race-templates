# Moved

The locked AI contract is [`TEMPLATE_CONTEXT_SCHEMA.md`](TEMPLATE_CONTEXT_SCHEMA.md)
(`<RaceFolder>/CONTEXT.md`).

Race-folder `README.md` is a laconic human page, not the AI source of truth.
